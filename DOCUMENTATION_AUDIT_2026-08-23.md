# Documentation Audit — 2026-08-23

**Scope:** accuracy/currency (точность и актуальность) and completeness/structure (полнота и структура) of this repository's documentation (`*.md`, README, `docs/`), assessed against a snapshot of the default branch on 2026-08-23. This is a documentation snapshot audit, not a code-quality or security review, and does not cover unmerged branches.

## Overall health assessment

**Fair.** For a 12-file specification repo, the documentation is unusually disciplined internally: a closed status vocabulary, a five-value loss taxonomy, and the SC-01…SC-16 threat/scenario IDs are all defined once and repeated consistently across files, no internal relative markdown links are broken, and no file is empty or a stub. What drags this down from "good" is that the repo's own currency/traceability machinery has already drifted from the live tree it describes: a newly committed file is invisible to every navigation surface (README map, AI-context reading order, future-work ledger), the ledger's "fresh audit" checkpoint is already stale relative to that commit, and the Provenance Matrix — the document whose entire job is completeness — only tracks 2 of the 12 invariants it should. These are exactly the silent-drift failure modes the repository's own philosophy (`SOURCE UPDATE ≠ AUTOMATIC COMPOSITION UPDATE`) warns against, now showing up inside the meta-documentation itself.

## Findings

1. **docs/research/EITI_COMPOSITION_RESEARCH_INPUT.md** | structure | high | This file is a complete orphan: no other document links to it, lists it, or includes it in the prescribed reading order. | README.md's "Repository map" (mirrored in README.ru.md) enumerates `docs/` as exactly 8 files with no `research/` subdirectory, and `docs/AI_CONTEXT.md`'s numbered 10-step "Reading order" never mentions it either.

2. **docs/CURRENT_STATUS.md** | accuracy | high | The "fresh audit" this document (and docs/AUDIT_AND_FUTURE_WORK.md) presents as current is already behind the actual repo HEAD. | Every ledger item is stamped `Last verified: 2026-08-16` and AUDIT_AND_FUTURE_WORK.md §3 calls itself "fresh audit 2026-08-16," but the only reachable commit is `47b2f0a` ("docs(research): add EITI composition research input"), dated 2026-08-21 — and neither document's "what exists" section mentions that file.

3. **docs/PROVENANCE_MATRIX.md** | completeness | high | The matrix states "Every normative rule therefore records origin, source status, local status, scope, rationale, and supersession state," but §5 "Local composition rules" contains only two entries (MK-01, MK-02) against the 12 invariants defined in docs/COMPOSITION_INVARIANTS.md. | Ten invariants have no provenance record, and the MK-*/NK-*/SOUL-* IDs used in this file never appear anywhere else in the repo.

4. **docs/CURRENT_STATUS.md** | accuracy | medium | A specific, unelaborated external-audit claim is asserted once and never substantiated elsewhere. | "External audit checkpoint: Manus AI read-only audit at historical PR head 0028ab4e5d115220a61c96353a00b8ed4722f487 → CHANGES_REQUIRED; findings F-01…F-06 were remediated before merge." "Manus" and "F-01…F-06" appear nowhere else.

5. **README.md** | accuracy | medium | The ownership table (mirrored verbatim in README.ru.md) disagrees with the supposedly identical matrix in docs/ARCHITECTURE.md §4. | README's Native Kernel "does not own" cell reads "cognition, identity admission, relationship truth," while ARCHITECTURE.md's version adds "consent."

6. **docs/AUDIT_AND_FUTURE_WORK.md** | completeness | low | The repository is described as public (MK-FW-002: "the audited tree contains no license file") with no LICENSE file present anywhere in the working tree; correctly self-flagged as an open decision (Issue #2) rather than a silent gap, but the standard file itself remains absent.

7. **README.md / docs/AI_CONTEXT.md / docs/PROVENANCE_MATRIX.md** | accuracy | low | The project name "Velantrim Native Kernel," used throughout the prose, differs by one letter from the GitHub org/repo handle "velantrian" used in every link. Both spellings are applied with full internal self-consistency, so this may be intentional, but it is never explained anywhere.

**Not found (checked, clean):** no broken internal relative markdown links, no empty/stub files, and no unresolved duplicate documents; README.md and README.ru.md are structurally parallel translations.

---
*Generated by an automated documentation audit (Claude Code).*
